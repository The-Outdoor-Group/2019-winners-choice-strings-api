<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @type.name %>
</p>

<% if admin_signed_in? %>
<%= link_to 'Edit', edit_type_path(@type), class: "btn btn-primary" %>
<% end %>
<%= link_to 'Back', types_path, class: "btn btn-secondary" %>
